#include "../include/FullMatrix.h"

using namespace orbsim

int main(){
    FullMatrix* A = new FullMatrix(2,3);
    FullMatrix* B = new FullMatrix(3,2);

    for(int x = 0;x<3;x++){
        for(int y = 0;y<2;y++){
            insertValue()
        }
    }
    return 0;
}